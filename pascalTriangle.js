var generate = function(numRows) {
    let res=[];
    for(let i=0;i<numRows;i++)
    {
        let temp=[];
        if(res[i]==undefined||res.length==0)
        {
            temp.push(1);
        }
        else
        {
            temp.push()
        }
    }
};

let numRows = 5;

console.log("Pascals Values:",generate(numRows));